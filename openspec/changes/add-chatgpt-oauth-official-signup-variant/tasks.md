## 1. 变体路由与配置

- [ ] 1.1 扩展 ChatGPT 注册模式与配置解析，使任务可以显式选择官网注册 GUI OAuth 变体，同时不改变当前默认 GUI 行为
- [ ] 1.2 定义并补充新变体所需的运行时配置项，包括目标工作空间上下文、Team 成员管理所依赖的凭据与能力边界，以及移除重试相关配置
- [ ] 1.3 将变体选择结果持久化到任务或账号结果元数据中，使操作者能够区分官网注册链路与旧的 CPA 入口 GUI 链路
- [ ] 1.4 按设计产物中的目标目录结构创建或调整对应文件落点，确保模式路由、引擎编排、工作流、step 与 helper 各自归位
- [ ] 1.5 实现“只有当前端提供 Team 会员账号时才启用官网注册变体，否则回退原 GUI 链路”的判定逻辑，并补充对应元数据

## 2. 官网注册工作流实现

- [ ] 2.1 补充打开 ChatGPT 官网注册入口并驱动密码提交前账号创建过程所需的目标目录项与 step 实现
- [ ] 2.2 重构或扩展注册工作流编排，使官网注册变体复用现有密码后的注册尾段，而不是复制 OTP、资料补全与 consent 逻辑
- [ ] 2.3 在 GUI 流程上下文与引擎中新增官网注册阶段及其失败模式的阶段状态记录
- [ ] 2.4 新增 `workflows/official_signup_workflow.py` 以及 `steps/official_signup/` 目录下的官网注册前半段 step 文件，并保持与现有 registration/login 目录职责一致

## 3. 工作空间加入与登录链路复用

- [ ] 3.1 实现或接入 Team 工作空间成员管理 helper 边界，使其能在登录续跑前把新注册账号加入目标工作空间
- [ ] 3.2 扩展 GUI 引擎编排，在注册成功后执行工作空间加入，并在加入失败时以明确的阶段错误终止流程
- [ ] 3.3 在工作空间加入成功后复用现有 GUI 登录 OAuth 工作流，并为新变体记录分阶段的登录与 OAuth 结果
- [ ] 3.4 新增或补齐 `platforms/chatgpt/team_workspace.py`，将工作空间加入与移除逻辑从 GUI step 中隔离为独立 helper 边界
- [ ] 3.5 确保只有在工作空间加入成功后才会把当前任务标记为需要执行强制移除清理

## 4. 清理流程与结果语义

- [ ] 4.1 实现登录阶段结束后的强制清理动作：无论登录成功或失败，只要已加入 Team 工作空间，就必须触发成员移除
- [ ] 4.2 在 Team 工作空间 helper 中实现成员移除的多次重试策略，并把重试次数与最终结果写入元数据
- [ ] 4.3 调整最终结果收口逻辑，确保成员移除成功是任务结束前的必要条件；若移除最终失败，则整个任务失败
- [ ] 4.4 为“移除失败后切换新的 Team 会员账号继续邀请与移除”的补偿能力预留上下文与扩展口，但本次不实现补偿执行
- [ ] 4.5 更新结果与扩展元数据生成逻辑，使注册、工作空间加入、OAuth 登录复用和清理分别输出独立状态，并能明确区分“主流程失败”与“清理失败导致任务失败”

## 5. 验证

- [ ] 5.1 新增或更新单元测试，覆盖注册模式路由、官网注册密码前分支，以及密码后尾段复用行为
- [ ] 5.2 新增或更新引擎级测试，覆盖“缺少 Team 会员账号时回退原 GUI 链路”的行为
- [ ] 5.3 新增或更新引擎级测试，覆盖工作空间加入成功/失败、登录链路复用执行，以及无论登录成败都必须触发清理的结果语义
- [ ] 5.4 新增或更新测试，覆盖成员移除多次重试后成功、重试后失败导致整任务失败、以及补偿扩展口上下文生成
- [ ] 5.5 运行相关的 ChatGPT GUI/OAuth 测试集，并修复新变体引入的回归问题
- [ ] 5.6 新增与目标目录结构对应的测试文件，至少覆盖官网注册工作流与 Team 工作空间 helper 两个新落点
